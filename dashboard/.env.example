# Dashboard server port
PORT=3000

# RCON connection settings
RCON_HOST=localhost
RCON_PORT=25575
RCON_PASSWORD=

# Path to pumpkin.toml config file
CONFIG_PATH=../pumpkin.toml

# Dashboard authentication token (if set, all /api routes require Bearer token)
DASHBOARD_TOKEN=
